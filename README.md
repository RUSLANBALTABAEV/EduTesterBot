# 🎓 EduTester Bot - Telegram бот для онлайн-тестирования

Полнофункциональный бот для проведения тестирования с поддержкой трёх языков (русский, английский, узбекский).

## 📋 Возможности

### Для студентов:
- ✅ Регистрация с загрузкой фото и документов
- 🔐 Авторизация по номеру телефона
- 📚 Просмотр доступных тестов
- ✍️ Прохождение тестов с разными типами вопросов:
  - Одиночный выбор (single choice)
  - Множественный выбор (multiple choice)
  - Текстовый ответ (text answer)
- ⏱ Таймер для тестов с ограничением времени
- 📊 Просмотр результатов тестирования
- 💾 Сохранение результатов в файл
- 🌐 Мультиязычность (RU/EN/UZ)

### Для администраторов:
- 👥 Управление пользователями
- ➕ Создание тестов вручную
- 📤 Загрузка тестов из Excel
- 📝 Добавление вопросов
- ✏️ Редактирование тестов
- 📊 Просмотр статистики
- 📥 Экспорт результатов в Excel
- 🔄 Активация/деактивация тестов

## 🚀 Быстрый старт

### 1. Требования

- Python 3.9+
- PostgreSQL или SQLite
- Telegram Bot Token

### 2. Установка

```bash
# Клонируем репозиторий
git clone https://github.com/yourusername/edutester-bot.git
cd edutester-bot

# Создаём виртуальное окружение
python -m venv venv

# Активируем виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 3. Настройка

Создайте файл `config/.env`:

```env
TOKEN=your_telegram_bot_token_here
ADMIN_ID=your_telegram_user_id
SQLALCHEMY_URL=sqlite+aiosqlite:///./test.db
```

Для PostgreSQL используйте:
```env
SQLALCHEMY_URL=postgresql+asyncpg://user:password@localhost/dbname
```

### 4. Инициализация базы данных

```bash
python setup.py
```

### 5. Запуск

```bash
python main.py
```

## 📝 Структура проекта

```
edutester-bot/
├── config/              # Конфигурация
│   ├── .env            # Переменные окружения (создать вручную)
│   └── bot_config.py   # Настройки бота
├── db/                 # База данных
│   ├── models.py       # Модели SQLAlchemy
│   └── session.py      # Сессии БД
├── fsm/                # FSM состояния
│   ├── auth.py         # Авторизация
│   ├── registration.py # Регистрация
│   └── test.py         # Тестирование
├── handlers/           # Обработчики
│   ├── start.py        # /start и выбор языка
│   ├── auth.py         # Авторизация
│   ├── registration.py # Регистрация
│   ├── testing.py      # Тестирование
│   ├── test_results.py # Результаты
│   ├── admin.py        # Админ-панель
│   └── admin_testing.py# Управление тестами
├── i18n/               # Локализация
│   └── locales.py      # Переводы (RU/EN/UZ)
├── keyboards/          # Клавиатуры
│   ├── reply.py        # Reply клавиатуры
│   └── inline.py       # Inline клавиатуры
├── main.py             # Точка входа
├── setup.py            # Инициализация БД
└── requirements.txt    # Зависимости
```

## 🎯 Использование

### Регистрация пользователя

1. Запустите бота `/start`
2. Выберите язык
3. Нажмите "Регистрация"
4. Введите имя
5. Введите возраст
6. Отправьте номер телефона
7. Отправьте фото
8. Отправьте документ (PDF/JPG/PNG)

### Прохождение теста

1. Авторизуйтесь (если ещё не авторизованы)
2. Выберите "Тесты" или "Мои тесты"
3. Выберите тест из списка
4. Отвечайте на вопросы
5. Для множественного выбора нажмите "✅ Готово"
6. Для текстовых вопросов введите ответ
7. Получите результаты

### Создание теста (администратор)

#### Вручную:

1. Выберите "Управление тестами"
2. Нажмите "➕ Создать тест"
3. Введите данные теста
4. Нажмите "📝 Добавить вопросы"
5. Выберите тест
6. Введите текст вопроса
7. Выберите тип вопроса
8. Укажите баллы
9. Введите варианты ответа (формат: `*Правильный||Неправильный`)

#### Из Excel:

1. Скачайте шаблон "📥 Скачать шаблон Excel"
2. Заполните файл:
   - **question**: текст вопроса
   - **type**: single/multiple/text
   - **points**: количество баллов
   - **options**: варианты через `||`, правильные с `*`
3. Загрузите файл "📤 Загрузить тест из Excel"

#### Пример Excel файла:

| question | type | points | options |
|----------|------|--------|---------|
| Столица России? | single | 2 | *Москва\|\|Санкт-Петербург\|\|Казань |
| Выберите чётные числа | multiple | 3 | *2\|\|3\|\|*4\|\|5 |
| Опишите фотосинтез | text | 5 | - |

## 🔧 API и функции

### Типы вопросов

```python
# Single choice - одиночный выбор
question_type = "single"
# Пользователь выбирает один вариант

# Multiple choice - множественный выбор
question_type = "multiple"  
# Пользователь выбирает несколько вариантов

# Text answer - текстовый ответ
question_type = "text"
# Пользователь вводит ответ текстом
```

### Подсчёт баллов

```python
# Single: полные баллы если ответ правильный
if selected_option in correct_options:
    score += question.points

# Multiple: полные баллы за полное совпадение,
# частичные за частичное совпадение
if selected == correct:
    score += points
elif selected.issubset(correct):
    score += points * len(selected) / len(correct)

# Text: требует ручной проверки (не оценивается автоматически)
```

## 📊 База данных

### Модели:

- **User**: пользователи
- **Test**: тесты
- **Question**: вопросы
- **Option**: варианты ответов
- **TestResult**: результаты тестирования

### Диаграмма связей:

```
User ──< TestResult >── Test
              │
         Question ──< Option
```

## 🌐 Локализация

Поддерживаемые языки:
- 🇷🇺 Русский (ru)
- 🇺🇸 English (en)
- 🇺🇿 O'zbek (uz)

Добавить новый язык:
1. Откройте `i18n/locales.py`
2. Добавьте код в `AVAILABLE_LANGUAGES`
3. Добавьте переводы в `TRANSLATIONS`

## ⚙️ Конфигурация

### Переменные окружения (.env):

```env
# Обязательные
TOKEN=                  # Telegram Bot Token
ADMIN_ID=               # Telegram User ID администратора
SQLALCHEMY_URL=         # URL базы данных

# Примеры SQLALCHEMY_URL:
# SQLite: sqlite+aiosqlite:///./test.db
# PostgreSQL: postgresql+asyncpg://user:pass@host/db
# MySQL: mysql+aiomysql://user:pass@host/db
```

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте токен в `.env`
- Убедитесь, что бот запущен
- Проверьте логи в консоли

### Ошибки базы данных
- Запустите `python setup.py`
- Проверьте `SQLALCHEMY_URL`
- Проверьте права доступа к БД

### Проблемы с кодировкой
- Используйте UTF-8 для всех файлов
- В Windows: `chcp 65001` перед запуском

### Excel не загружается
- Проверьте формат файла (.xlsx)
- Убедитесь в наличии листа 'Questions'
- Проверьте названия колонок

## 📈 Будущие улучшения

- [ ] Поддержка изображений в вопросах
- [ ] Экспорт тестов в различные форматы
- [ ] Статистика по каждому вопросу
- [ ] Таймер для каждого вопроса отдельно
- [ ] Случайный порядок вопросов/ответов
- [ ] Групповые тесты
- [ ] Уведомления о новых тестах
- [ ] Web-интерфейс для администраторов

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 👨‍💻 Автор

EduTesterBot Team

## 📞 Поддержка

- 📧 Email: support@edutester.bot
- 💬 Telegram: @edutester_support
- 🐛 Issues: [GitHub Issues](https://github.com/yourusername/edutester-bot/issues)

---

⭐ Если проект полезен, поставьте звезду на GitHub!
